# Manual CI utilities

These files are not referenced by the active GitLab CI configuration. They are preserved here for manual workflows and later cleanup.

Before deleting a file, check documentation, scheduled automation, and external callers that may invoke it directly.
